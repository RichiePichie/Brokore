import 'dart:ui';

class AppColors {
  static const background = Color(0xFF0F0F1A);
  static const surface = Color(0xFF1C1C2E);
  static const primary = Color(0xFFE53935); // Red (actions, buttons)
  static const accent = Color(0xFFFDD835); // Yellow (PR, highlights)
  static const text = Color(0xFFF3F4F6);
  static const textMuted = Color(0xFF9CA3AF);
  static const divider = Color(0xFF2A2A3D);
}
